using System;

namespace Roblox.Instances;

public class Actor : Model
{
    // Methods
    public object BindToMessage(string? topic, Delegate? function) => null!;
    public object BindToMessageParallel(string? topic, Delegate? function) => null!;
    public object SendMessage(string? topic, object[]? message) => null!;

}
